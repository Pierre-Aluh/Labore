[project]
name = "labore-portal-api"
version = "0.1.0"
description = "Fundacao da API do Labore Portal"
requires-python = ">=3.12"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.mypy]
strict = true
